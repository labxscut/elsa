# 汇报提示卡 - 图表快速参考

## 🎯 核心要点

**所有误差棒和阴影都表示：标准差 (Standard Deviation, SD)**
- **不是**标准误 (SE)
- **不是**置信区间 (CI)
- **不是**极值范围

**样本量：每个 alpha 值重复 50 次独立实验**

---

## 📊 各子图快速解读

### 1️⃣ LLA Score（左上）
**竖线 = Mean ± 1 SD**
```
"误差棒显示 50 次实验的标准差，约为均值的 10-15%，
表明算法性能稳定。"
```

### 2️⃣ Window Overlap（右上）
**竖线 = Mean ± 1 SD**
```
"标准差约 0.05，说明定位精度在不同数据实例上
表现一致。"
```

### 3️⃣ Boundary Errors（左中）⭐ **重点**
**阴影 = Mean ± 1 SD 区间（~68% coverage）**
```
"阴影区域代表均值加减一个标准差。如果误差服从正态分布，
约 68% 的检测结果会落在此范围内。我们观察到对称的窗口
膨胀模式：起始边界平均提前 4-6 个索引，结束边界平均延后
4-6 个索引。"
```

### 4️⃣ FDR（右中）
**柱状图 = FDR 均值；数字 = 假阳性计数**
```
"标注显示 50 个对照实验中的假阳性数量。FDR 保持在
0-9.1%，表明 90% 以上的检测结果为真阳性。"
```

### 5️⃣ Batch Time（左下）
**无误差标记**
```
"每个点代表该条件下 100 次实验的总时间。Alpha ≥ 0.88 后
时间降低 40%，可能是信号增强允许算法剪枝优化。"
```

### 6️⃣ Window Inflation（右下）
**派生指标**
```
"基于均值边界误差计算，显示检测窗口系统性膨胀约 10-13%，
与观察到的对称误差一致。"
```

---

## 💬 标准话术模板

### 开场
> "这组图展示了 **11 个关联强度下各重复 50 次独立实验** 的结果。图中的 **误差棒和阴影表示标准差**，反映算法在不同数据实例上的表现变异性。"

### 解释阴影（如被问到）
> "阴影区域是 **mean ± 1 SD**，在正态分布假设下约 **68% 的观测值** 会落在此区间。我们选择展示 SD 而非置信区间，是为了直观体现 **单次实验可能的波动范围**。由于样本量为 50，均值的标准误仅为 SD/√50，估计精度已经很高。"

### 关键发现
> "三个核心观察：
> 1. **LLA 分数稳定**：标准差小，说明算法对随机性不敏感
> 2. **对称窗口膨胀**：起始提前约 5 个索引，结束延后约 5 个索引
> 3. **低假发现率**：FDR < 10%，精确度超过 90%"

---

## ❓ 常见提问应对

### Q: "为什么不用 95% 置信区间？"
✅ **回答**：
> "我们的目标是展示 **数据的内在变异性**（不同实例间的差异），而非参数估计的不确定性。SD 更能体现算法在实际应用中的性能波动。若绘制 95% CI（约 ±0.28 SD），区间会非常窄，无法充分展示这种变异性。"

### Q: "样本量 50 够吗？"
✅ **回答**：
> "对于估计均值，n=50 提供充分的统计效力。标准误 SE = SD/√50 ≈ 0.14×SD，因此均值的 95% CI 远小于图中的 SD 范围。我们有信心均值估计是可靠的。"

### Q: "为何某些 alpha 下 FDR=0？"
✅ **回答**：
> "在 50 个对照实验中未观测到假阳性（FP=0），因此 FDR = 0/(TP+0) = 0。这表明特异性极高，但不代表真实 FDR 绝对为零——只是低于我们当前的检测阈值（< 1/50 = 2%）。"

### Q: "窗口膨胀是算法缺陷吗？"
✅ **回答**：
> "这是算法的 **保守策略**，倾向于略微扩大检测窗口以避免漏掉边界信号。10-13% 的膨胀在 IoU 仍达 89% 的情况下是可接受的。我们可以通过调整边界延伸惩罚参数来减少膨胀，但可能以召回率为代价。"

---

## 🔢 关键数据速查

| 指标 | 范围 | 解读 |
|------|------|------|
| **LLA Score** | 0.45-0.48 (SD ~0.05) | 稳定、强关联信号 |
| **Window Overlap** | 0.88-0.90 (SD ~0.05) | 高定位精度 |
| **Start Error** | -4 至 -6 (SD ~3.5-4.0) | 系统性提前 |
| **End Error** | +4 至 +6 (SD ~3.5-4.0) | 系统性延后 |
| **FDR** | 0-9.1% | 高精确度 (90%+ 真阳性) |
| **Inflation** | 1.10-1.13 | 轻度膨胀 (+10-13%) |

---

## 📝 汇报时避免的错误说法

❌ **错误**: "误差棒显示置信区间"  
✅ **正确**: "误差棒显示标准差"

❌ **错误**: "阴影区域是所有可能的取值范围"  
✅ **正确**: "阴影是均值加减一个标准差，约覆盖 68% 的观测值（正态假设下）"

❌ **错误**: "标准差越小说明算法越准确"  
✅ **正确**: "标准差越小说明算法在不同数据实例上的表现越一致"

❌ **错误**: "FDR=0 说明完全没有假阳性"  
✅ **正确**: "50 次对照实验中未观测到假阳性，FDR 低于检测下限（<2%）"

---

## 🎤 完整汇报示例（60 秒）

> "这张图总结了我们在 **11 个关联强度下各重复 50 次实验** 的结果。所有 **误差棒和阴影表示标准差**，反映算法在不同数据上的变异性。
> 
> 左上图显示 **LLA 分数随 alpha 增加而温和上升**，误差棒较小表明算法稳定。右上图的 **窗口重叠度持续在 89% 左右**，说明定位精度高。
> 
> 左中图是关键：阴影是 **mean ± 1 SD 区间**，在正态假设下约覆盖 68% 的观测值。我们看到 **对称的边界偏差** ——起始平均提前 5 个索引，结束平均延后 5 个索引，形成 **10-13% 的系统性窗口膨胀**（右下图）。这是算法的保守策略，可通过参数调整优化。
> 
> 右中图的 **假发现率始终低于 10%**，表明检测具有高精确度。左下图显示 alpha 增大后 **计算时间降低 40%**，可能源于信号增强允许的剪枝优化。
> 
> 综合来看，LLA 算法在检测和定位性能上都表现出 **良好的鲁棒性和可重复性**。"

---

## ⚡ 一句话总结

**"误差棒/阴影 = 标准差（50 次实验）→ 反映算法的稳定性，不是估计精度"**

记住这句，其他都好办！🎯
